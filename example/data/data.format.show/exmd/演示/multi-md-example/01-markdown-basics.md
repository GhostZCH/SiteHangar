---
title: Markdown 基础格式
subtitle: 标准 Markdown 语法与扩展功能展示
---

### 段落与内联格式


标准 Markdown 段落，连续非空行自动合并为一个段落。

**加粗文本**用于强调重要内容，*斜体文本*用于表示术语或引用，`行内代码`用于展示代码片段，[链接文字](https://example.com)用于跳转到外部资源。

还可以组合使用：***加粗且斜体***，**`加粗代码`**，以及行内公式 $E=mc^2$。

### 列表

无序列表适合展示并列项目：

- 项目 A：基础功能项
- 项目 B：扩展功能项
- 项目 C：高级功能项

### 引用块

引用块用于强调重要内容：

> 这是一段引用文字的示例，这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例这是一段引用文字的示例。

### 图片

展示插入图片：

![image](image/landscape.jpg)


### 代码块

代码块用于展示多行代码，下面是一个冒泡排序的实现：

```python
def bubble_sort(arr):
    n = len(arr)
    // 外层循环控制遍历轮数
    for i in range(n):
        // 标记本轮是否发生交换
        swapped = False
        // 内层循环进行相邻元素比较
        // 每轮结束后，最大的元素会"冒泡"到末尾
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                // 交换相邻元素
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        // 如果本轮没有发生交换，说明已经有序，提前退出
        if not swapped:
            break
    return arr
```

### 表格

表格用于展示结构化数据：

| 气候类型 | 分布区域 | 主要特征 | 年均降水量（mm） |
|----------|----------|----------|-----------------|
| 温带季风 | 秦岭-淮河以北 | 夏热冬寒 | 400-800 |
| 亚热带季风 | 秦岭-淮河以南 | 夏热冬暖 | 800-1600 |
| 热带季风 | 海南、滇南 | 全年高温 | 1600-2000 |
